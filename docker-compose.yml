services: 
  dev:
      image: mlops-env-dev
      stdin_open: true # docker run -i
      tty: true        # docker run -t
      hostname: dev
      restart: always
      environment:
        DEV_LOCAL_IP: 172.28.1.7
        DEV_LOCAL_HOSTNAME: dev
      volumes:
        - ./dev/home:/home/developer
        - ../Data:/landing
        - ../repos:/home/repos
      networks:
        sparknet:
          ipv4_address: 172.28.1.7
      command: /bin/bash


volumes:
  pg_data:

networks:
  sparknet:
    ipam:
      driver: default
      config:
        - subnet: 172.28.0.0/16